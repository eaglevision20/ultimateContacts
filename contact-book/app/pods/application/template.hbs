<header>
  {{#if session.content.access_token}}
      <a {{ action 'logout' }}>Logout</a>
  {{else}}
    {{#link-to 'contactbook'}}Login{{/link-to}}
  {{/if}}
  {{#if session.content.siteAdmin}}
    {{#link-to 'admin'}}Site Administration{{/link-to}}
  {{/if}}
</header>
<div class="main">
  {{outlet}}
</div>